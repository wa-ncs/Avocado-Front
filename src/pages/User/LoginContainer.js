import React from 'react';

import { Layout, Card } from 'antd';
const { Content } = Layout;
const LoginContainer = () => {
	return <Content style={styleContent}>helo</Content>;
};

export default LoginContainer;

const styleContent = {
	display: 'flex',
	background: 'white',
	padding: '15px 15%',
};
